#!/usr/bin/env python
# coding=utf-8

height = float(input('请输入身高:'))             #保存身高的变量:单位(米)
weight = float(input('请输入体重:'))             #保存体重的变量:单位(kg)

bmi = weight /(height *height);
print(bmi)


